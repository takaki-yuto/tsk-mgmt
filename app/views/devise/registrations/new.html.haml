.backimage-body
  = form_for(resource, as: resource_name, url: registration_path(resource_name),html: { class: "form-body" }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field-contener
      .registration 新規登録
      .registration-form
        .form-box
          .form-group
            = f.label :名前
            = f.text_field :name, autofocus: true, maxlength: "6", class: "text-field", placeholder: "最大6文字まで入力できます"
          .form-group
            = f.label :メールアドレス
            = f.email_field :email, autofocus: true, autocomplete: "email", class: "text-field", placeholder: "PC・携帯どちらでも可"
          .form-group
            = f.label :パスワード
            = f.password_field :password, autocomplete: "new-password", class: "text-field", placeholder: "6文字以上で入力してください"
          .form-group
            = f.label :確認のため、再度パスワードを入力してください
            = f.password_field :password_confirmation, autocomplete: "new-password", class: "text-field", placeholder: "もう一度入力してください"
          .actions
            = f.submit "登録", class: "next-btn"
